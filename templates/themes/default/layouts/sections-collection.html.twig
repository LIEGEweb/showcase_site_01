{% extends 'themes/'~app_theme~'/layouts/single-services-container.html.twig' %}

{% block content %}
    {% for item in items %}
        {% if loop.index is divisible by(2) %}
            {% set class='float-right md:ml-6' %}
        {% else %}
            {% set class='float-left md:mr-6' %}
        {% endif %}
        {#        {{ item is odd }} #}
        {#        {% if item is even %} #}
        {#            {% set align = "right" %} #}
        {#        {% else %} #}
        {#            {% set align = "left" %} #}
        {#        {% endif %} #}

        {% block item %}
        {% endblock %}
    {% endfor %}

    <div class="navigation">
        {{ knp_pagination_render(items) }}
    </div>

{% endblock %}